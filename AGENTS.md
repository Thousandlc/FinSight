《知数（FinSight）》开发规则

1. 不擅自改变产品需求

2. 不擅自新增功能

3. 不擅自改变数据库模型

4. 涉及数据库修改必须说明 Migration

5. 涉及财务计算必须增加 Unit Test

6. AI 不允许直接修改核心财务数据

7. AI 不负责核心金额计算

8. 所有 AI 输出必须经过 DTO / Validator

9. 不允许把 API Key 写入代码

10. 不允许把真实财务数据写入测试代码

11. 不允许为了修一个 Bug 大规模重构

12. 每次修改前先解释计划

13. 修改后运行相关测试

14. 测试失败不能宣称完成

15. 每次只完成当前任务
